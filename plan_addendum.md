# PLAN ADDENDUM — Feedback integration + current status

Date: 2026-08-30. Companion to `plan` / `phản hồi`. Captures the 5 reviewer refinements and what was actually done in code.

## 1. Reviewer feedback (from `phản hồi`) — resolution

1. **Policy enum + groupId relation** — RESOLVED in `AUTORELOG.Core.psm1::Get-ClientPolicy`:
   `fixed` -> always-on (`running`), `none` -> `ignore`, `orphan` (client_68 / `*Ghost Story PC*`) -> `blocked`, `scheduled` -> group window. Policy is derived from `group`; explicit `policy` field on a master client is honored if present.
2. **Agent uses protocol, NOT menu script** — Recorded as hard rule (plan §22 + `dulieu` §3): the new Agent must drive `row_toggle`/WebSocket protocol captured from HAR, never click UI/menu coordinates of old scripts.
3. **🔒 Auth for control API** — MANDATORY before any public exposure (ngrok). Every control endpoint (`start`/`stop`/`bulk-action`) MUST require a shared-secret header (e.g. `X-AUTORELOG-KEY`). Dashboard is read-only until auth lands. (Not yet coded — Agent v1 is observation-only; control path blocked on PC gates B1/B2/B3.)
4. **Dual-run / canary migration from AutoCycle** — Plan §7 updated intent: Agent runs read-only alongside AutoCycle; enable control for ONE group as canary only after state matches; only then disable AutoCycle. No cut-over before that.
5. **Timezone / `days`** — `Get-DesiredState` now evaluates against `Asia/Ho_Chi_Minh` (Windows: `SE Asia Standard Time` fallback), not local clock. Schedule times are group-local. `days:[1..7]` (1=Monday) to be added when per-day scheduling is implemented.

Additional code-level fixes applied:
- **Orphan `desiredState` bug**: `Get-ClientPolicy`/`Get-DesiredState` now detect orphan via `clientId`/`name` (not just a `client` field), so `client_68` computes `blocked` (was `ignore`). Verified by `Sync-Discovery`.
- **B3 `key` mapping**: `Resolve-KeyToClient` maps `key` (hypothesis `root/<sess>#<idx>`) to `clientId` through the live snapshot `idx` — never hardcode `key`.

## 2. `clients_master.json` created (B4)
Generated from `runtime_registry.json` + `lich trinh tat mo.xlsx` windows:
- schedule: `gr1=04:00, gr2=08:00, gr3=12:30, gr4=19:45` (gr4 wraps +1d).
- 27 clients: 5 `fixed`, 20 `scheduled` (5 per gr1–gr4), `client_51`=`none`, `client_68`=`none` (orphans). `slot` = group open time.
- `Validate-Master.ps1` => **PASS** (no duplicate, no group conflict, orphan hard-block ok, windows valid).

## 3. Phase 0 gate status (regenerated by `B1-B7-Validate.ps1`)
| Gate | Status |
|---|---|
| B4 master validation / wraparound | PASS |
| B5 group conflict (1 client → 1 group) | PASS_STRUCT |
| B6 client_68 orphan | STRUCT_PASS_LIVE_PENDING |
| B1 stable client_id across restart | PENDING_PC_TEST |
| B2 row_toggle Start/Stop correlation | PENDING_PC_TEST |
| B3 key/epoch semantics | PENDING_PC_TEST |
| B7 heartbeat/reconnect measured | PENDING_PC_TEST |

## 4. What can run now (no PC) vs what needs live PC
- **Now**: master validation, master↔runtime reconcile, schedule-window + desiredState computation, gate reporting, XLSX→master pipeline.
- **Needs PC/Remote**: B1 (restart Auto/PC), B2 (1-client toggle correlation), B3 (live key/epoch capture), B7 (real heartbeat). Agent v1 control path stays disabled until these PASS.

## 5. SECURITY ACTION
`remote.txt` previously held a plaintext `remote.360auto.net` password. It is now redacted and `remote.txt` added to `.gitignore`. **Rotate that password immediately** — it remains in git history of the public repo.
